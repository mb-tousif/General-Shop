import { Box } from '@mui/material';
import React from 'react';

const TotalCartBalance = () => {
    return (
        <Box>
            Hello
        </Box>
    );
};

export default TotalCartBalance;